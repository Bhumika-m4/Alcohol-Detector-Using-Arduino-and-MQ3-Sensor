// Pin configuration
const int sensor Pin = A0; // MQ-3 sensor analog output pin const int buzzer Pin = 8; //
Buzzer pin
const int led Pin = 9; // LED pin
// Threshold value for alcohol detection (adjust based on calibration) const int alcohol
Threshold = 300;
void setup () {
pin Mode (buzzer Pin, OUTPUT); pin Mode (led Pin, OUTPUT);
Serial. Begin (9600); // Initialize serial communication for debugging
}
void loop () {
int sensor Value = analog Read (sensor Pin); // Read MQ-3 sensor value Serial.
print("Alcohol Level: ");
Serial. Print ln (sensor Value); // Print sensor value for debugging
if (sensor Value > alcohol Threshold) {
digital Write (led Pin, HIGH); // Turn on LED digital Write (buzzer Pin, HIGH); // Turn on
buzzer
} else {
Digital Write (led Pin, LOW); // Turn off LED Digital Write (buzzer Pin, LOW); // Turn off
buzzer
}
Delay (100); // Short delay for stability
}



# Arduino Code Explanation

The Arduino program begins by defining input and output pins. The MQ3 sensor is connected to an analog input pin, while the buzzer and LED are connected to digital output pins.

During setup, the Arduino configures the buzzer and LED pins as outputs. Serial communication is also initialized to monitor sensor values during testing and debugging.

Inside the main loop, the Arduino continuously reads the analog value generated by the MQ3 sensor. These readings represent the detected alcohol concentration.

The program compares the sensor value against a predefined threshold. If the value exceeds the threshold, the LED and buzzer are turned ON to indicate alcohol detection. If the value remains below the threshold, both outputs remain OFF.

This simple logic enables real-time alcohol monitoring while maintaining low computational complexity. The code can be extended further by integrating GSM modules, LCD displays, IoT platforms, or vehicle ignition control systems.
